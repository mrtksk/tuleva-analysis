#Uncomment the next section and add new portfolios for use:

# list_portfolios <- list(port1 = tibble(stocks = c("ACWI","IGOV", "AGG"), wts = c(0.5, 0.25, 0.25)),
#                         port2 = tibble(stocks = c("ACWI","IGOV", "AGG"), wts = c(0.25, 0.5, 0.25)),
#                         port3 = tibble(stocks = c("ACWI","IGOV", "AGG"), wts = c(0.25, 0.25, 0.5)))
# saveRDS(object = list_portfolios, file = "./data/portfolios.rds")
